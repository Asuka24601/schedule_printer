<!--pages/select/select.wxml-->
<!-- <text>pages/select/select.wxml</text> -->
<view class="container">
  <!-- <view class="lever">
    <view class="icon_cont">
      <view class="left_icon">
      </view>
    </view>
    <view class="icon_cont">
      <view class="mid_icon">
      </view>
    </view>
    <view class="icon_cont">
      <view class="right_icon">
      </view>
    </view>
  </view> -->
  <view class="table_grid table_area" style="grid-template: {{style_temlpate}};">
    <view class="top_bar_items">
      {{this_week}}
    </view>
    <view class="top_bar_items" wx:for="{{week_day}}" wx:key="item">
    <view class="top_bar_items_up">
      {{week_name[index]}}
    </view>
    <view class="top_bar_items_down">
      {{item}}      
    </view>
    </view>
    <view class="x_area">
      <view wx:for="{{table_time}}" wx:key="item" class="top_bar_items">
        <view>
          {{item.s}}
        </view>
        <view>
          {{item.e}}
        </view>
      </view>
    </view>
    <view class="m_area">
      <view wx:for="{{currretTable}}" wx:key="item" class="m_area_items">
        <view wx:for="{{item}}" wx:for-key="row_item" wx:for-index="idx" class="top_bar_items">
          <view wx:if="{{currretTable[index][idx].name=='None'}}"></view>
          <view wx:else>
            {{currretTable[index][idx].name}}
          </view>
        </view>
      </view>
    </view>
  </view>
</view>